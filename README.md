# Edge AI for Smart Grid Demand Forecasting and Blackout Prevention

Student mini-project prototype using synthetic data.

This application trains a Random Forest model locally and performs local
inference. The risk layer is rule-based. It is not connected to a real
electrical grid and does not control electrical equipment.

## Run

```bash
pip install -r requirements.txt
python -m streamlit run app.py
```
